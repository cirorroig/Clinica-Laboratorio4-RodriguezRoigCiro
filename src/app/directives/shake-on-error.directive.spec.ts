import { ShakeOnErrorDirective } from './shake-on-error.directive';

describe('ShakeOnErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ShakeOnErrorDirective();
    expect(directive).toBeTruthy();
  });
});
