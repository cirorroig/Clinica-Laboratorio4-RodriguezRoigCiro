import { UserTypeBadgePipe } from './user-type-badge.pipe';

describe('UserTypeBadgePipe', () => {
  it('create an instance', () => {
    const pipe = new UserTypeBadgePipe();
    expect(pipe).toBeTruthy();
  });
});
